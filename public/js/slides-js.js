/*
// Create a class for the element
class PSlide extends HTMLDivElement {
  constructor() {
    // Always call super first in constructor
    super();
  }
}

// Define the new element
customElements.define('p-slide', PSlide);
*/
/*let animationList = [ { trigger: 'onclick', type: 'fadeIn', target: 'h4' },
                      { trigger: 'onclick', type: 'bounceIn', target: 'h5' } ];

$(document).click(function () {
    //alert("HI");
    if (animationList.length == 0) return;
    do {
        let item = animationList.shift();
        $(item.target).removeClass('hidden').addClass(item.type);
    } while (animationList.length > 0 && animationList[0].trigger != 'onclick');
});*/